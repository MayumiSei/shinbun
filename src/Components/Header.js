import React from 'react';
import '../Assets/style/index.scss';

const Header = (props) => {
    return(
        <header>
            <div className="container">
                <h1>Shinbun</h1>
            </div>
        </header>
    );
}

export default Header;